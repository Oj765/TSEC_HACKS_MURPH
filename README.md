
  # AI-Powered EdTech Website Design

  This is a code bundle for AI-Powered EdTech Website Design

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
